{
  "name": "cordova-plugin-azure-upload",
  "version": "1.1.0",
  "description": "Cordova plugin for uploading files to Azure Blob Storage with local notifications",
  "cordova": {
    "id": "cordova-plugin-azure-upload",
    "platforms": ["android", "ios"]
  },
  "scripts": {
    "build": "cordova build",
    "clean": "cordova clean",
    "test": "cordova run",
    "prepare": "cordova prepare"
  },
  "keywords": [
    "cordova",
    "azure",
    "blob",
    "upload",
    "plugin",
    "notifications"
  ],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "cordova-android": "^10.0.0",
    "cordova-ios": "^6.0.0",
    "@mauron85/cordova-plugin-background-geolocation": "^3.1.0",
    "cordova-plugin-advanced-http": "^3.1.0",
    "cordova-plugin-file": "^6.0.2"
  },
  "devDependencies": {
    "eslint": "^7.0.0",
    "jest": "^26.0.0"
  },
  "engines": {
    "cordovaDependencies": {
      "cordova-android": ">=10.0.0",
      "cordova-ios": ">=6.0.0"
    }
  }
}
